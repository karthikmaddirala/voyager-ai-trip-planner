# Copy this file to .env and paste your keys (do NOT commit .env — it's gitignored).

# Required — the planning agent, menus, feasibility, day plan, and copilot all use Claude.
ANTHROPIC_API_KEY = ""

# Optional — Google Places API (New): real ★ ratings + review counts for stops/attractions.
# Leave empty to keep the feature OFF (the app degrades gracefully to LLM judgement).
GOOGLE_API_KEY = ""
